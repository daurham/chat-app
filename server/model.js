const db = require('./db');

const getData = () => {
  db.query();
};
const postData = () => {
  db.query();
};
const updateData = () => {
  db.query();
};
const deleteData = () => {
  db.query();
};

module.exports = { getData, postData, updateData, deleteData };